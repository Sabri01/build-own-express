exports = module.exports = createApplication;

function createApplication() {
    var app = function(req,res,next) {
    };

    return app;
    //This returned app is what end user will get when they call express()
}
